---
openapi: "GET /quotes/{lago_id}"
authMethod: "bearer"
title: "Retrieve a quote"
---

<RequestExample>

```bash cURL
LAGO_URL="https://api.getlago.com"
QUOTE_ID="__QUOTE_ID__"
API_KEY="__YOUR_API_KEY__"

curl --location --request GET "$LAGO_URL/api/v1/quotes/$QUOTE_ID" \
  --header "Authorization: Bearer $API_KEY"
```

```python Python
from lago_python_client.client import Client
from lago_python_client.exceptions import LagoApiError

client = Client(api_key='__YOUR_API_KEY__')

try:
    client.quotes.find('__QUOTE_ID__')
except LagoApiError as e:
    repair_broken_state(e)  # do something on error or raise your own exception
```

```ruby Ruby
require 'lago-ruby-client'

client = Lago::Api::Client.new(api_key: '__YOUR_API_KEY__')

client.quotes.get('__QUOTE_ID__')
```

```js Javascript
await client.quotes.findQuote("__QUOTE_ID__");
```

```go Go
import (
	"context"
	"fmt"

	lago "github.com/getlago/lago-go-client"
)

func main() {
	lagoClient := lago.New().SetApiKey("__YOUR_API_KEY__")
	ctx := context.Background()

	quote, err := lagoClient.Quote().Get(ctx, "__QUOTE_ID__")
	if err != nil {
		// Error is *lago.Error
		panic(err)
	}

	// quote is *lago.Quote
	fmt.Println(quote)
}
```

</RequestExample>
