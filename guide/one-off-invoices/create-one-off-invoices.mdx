---
title: "Issue one-off invoices"
description: "Bill one or several add-ons to a customer immediately, without waiting for a billing period."
---

## Create a one-off invoice
To create a one-off invoice for a specific customer:

1. Select the customer from the list of customers; and
2. Click the **"Actions"** button located in the upper-right corner and select **"Create one-off invoice"**.

## Adding add-ons to one-off invoices
Once you have started the flow, add one or several add-ons to the invoice:

<Tabs>
  <Tab title="Dashboard">
    1. Define a purchase order number, if needed
    2. Click **"Add an add-on"** to add a line item to the invoice
    3. Set the number of units for the add-on
    4. Define the add-on billing period (past, present, or future)
    5. Edit the unit price, if needed (you can set it to 0)
    6. Edit the description displayed on the invoice
    7. In the payment settings, keep the inherited [payment term](#payment-terms) or choose one for this invoice
    8. Review the invoice and its payment term, then click **"Create"** to issue it

    <Frame caption="Adding add-ons to create a one-off invoice">
      <img src="/guide/one-off-invoices/images/one-off-invoices.png" />
    </Frame>
  </Tab>
  <Tab title="API">
  ```bash Assign add-ons to create a one-off invoice
  LAGO_URL="https://api.getlago.com"
  API_KEY="__YOUR_API_KEY__"
  curl --location --request POST "$LAGO_URL/api/v1/invoices" \
    --header "Authorization: Bearer $API_KEY" \
    --header 'Content-Type: application/json' \
    --data-raw '{
      "invoice": {
        "external_customer_id": "hooli_1234",
        "currency": "USD",
        "purchase_order_number": "po-1234",
        "fees": [
          {
            "add_on_code": "setup_fee",
            "units": 1,
            "unit_amount_cents": 50000,
            "description": "Implementation fee with a solution engineer",
            "from_datetime": "2025-01-01T00:00:00Z",
            "to_datetime": "2025-12-31T23:59:59Z"
          },
          {
            "add_on_code": "customer_success",
            "units": 2,
            "unit_amount_cents": 100000,
            "description": "One off customer success fee",
            "from_datetime": "2025-01-01T00:00:00Z",
            "to_datetime": "2025-12-31T23:59:59Z"
          }
        ]
      }
    }'
  ```
  </Tab>
</Tabs>

## Payment terms

You can choose a payment term for this invoice without changing the customer's default. In the payment settings, select a term and enter its days or month settings before issuing the invoice. If you keep inheritance, the customer's term applies, falling back to the billing entity's term when the customer has none of their own.

For example, your customer's subscription invoices use **Net 30**, but you agree **Net 15** for a one-off setup fee. Set Net 15 on the setup-fee invoice. If both invoices are dated **April 10, 2026**, the setup fee is due **April 25**, while the subscription invoice is due **May 10**.

See [Payment terms](/guide/invoicing/invoicing-settings/payment-term#choose-a-payment-term) for all six options and examples. The one-off invoice is issued immediately; the chosen term determines its payment deadline. Later changes to customer defaults do not change the term stored on this invoice.

## Application scope of one-off invoices
Keep the following in mind:

- One-off invoices are issued immediately and can include the same add-on multiple times.
- The currency and billing entity of the one-off invoice can differ from the customer's defaults: the invoice is issued in the currency and by the billing entity you select for it.
- You can use the same add-on to create one-off invoices for multiple customers whose subscriptions don't have the same currency or to apply a different amount for one of these customers.
- Coupons or prepaid credits (discounts) **do not apply** to one-off invoices.
- One-off invoices are subject to taxes, as defined in the customer view.

<Tip>

When using the API endpoint, if the amount and currency are null, Lago applies the default amount and currency defined in the UI.

</Tip>

## Invoicing

A **one-off invoice is invoiced straight away**. You can find one-off invoices via the `invoice.one_off_created` webhook message.
